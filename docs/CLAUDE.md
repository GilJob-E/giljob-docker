# Claude Notes for docs

Read the local `AGENTS.md` first.

Claude reminders:
- Keep AGENTS/CLAUDE prose English, while Korean product docs can remain Korean.
- Update `docs/architecture.noml` before regenerating SVG architecture output.
- Link to source-of-truth documents instead of duplicating them.
