# GilJobE owns STT + multimodal LiveKit subscriber logic AND the HTTP contract server
# (server/http_app.py: /subscriber/start|stop, /signals, /healthz, /readyz).
# Pin the integration source so Docker builds are reproducible. 88a4df5 adds the server
# entrypoint (python -m giljobe.server) and is the commit smoke-tested live against this stack.
git+https://github.com/GilJob-E/GilJobE.git@88a4df5
