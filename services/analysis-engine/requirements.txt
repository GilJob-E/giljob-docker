# GilJobE owns STT + multimodal LiveKit subscriber logic AND the HTTP contract server
# (server/http_app.py: /subscriber/start|stop, /signals, /healthz, /readyz).
# Pin the integration source so Docker builds are reproducible. 6bef78b fixes the 3s
# nonverbal lane silently skipping windows whose video frames arrive after the window
# closes (bursty LiveKit delivery): starved windows are now caught up when late frames
# land. It keeps the objective grounding lanes (vision MediaPipe + audio prosody,
# inject-and-emit) and the server entrypoint (python -m giljobe.server). The
# [vision,prosody] extras install the lane dependencies (mediapipe /
# praat-parselmouth+scipy); the lanes self-disable without them.
# Licence note: praat-parselmouth is GPL-3 — revisit before any public image distribution.
giljobe[vision,prosody] @ git+https://github.com/GilJob-E/GilJobE.git@6bef78b
