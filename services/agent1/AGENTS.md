# services/agent1 Agent Contract

This module is a future multimodal placeholder. Follow the root `AGENTS.md` plus these local rules.

## Current responsibilities
- Remain a scaffold placeholder until a planned multimodal slice is approved.
- Document the future boundary honestly: raw AV may be observed only inside the future Agent1 boundary.
- Emit structured signal outward in future designs; do not expose raw media, raw token values, JWTs, or provider secrets.

## Forbidden claims
Do not claim multimodal analysis, trajectory evaluation, production subscription to LiveKit tracks, or final-report integration is implemented here unless code and tests actually implement it.

## Tests
Use contract tests or add focused tests before changing this placeholder into runtime behavior.
